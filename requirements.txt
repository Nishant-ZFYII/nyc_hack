# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# Core data
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
scipy>=1.11.0
networkx>=3.0

# LLM clients
openai>=1.0.0
anthropic>=0.20.0

# Geocoding + HTTP
requests>=2.31.0
sodapy>=2.2.0

# ML / utilities
scikit-learn>=1.3.0
tqdm>=4.65.0

# Safety / Guardrails
# nemoguardrails>=0.11.0    # Full NVIDIA NeMo Guardrails (optional, for Colang flows)
# Our lightweight apply_guardrails() in guardrails/actions.py works without this

# Knowledge Graph Embeddings (optional — uses PyTorch)
# pykeen>=1.10.0

# Legacy UI (not used anymore, kept for compatibility)
# streamlit>=1.30.0

# NVIDIA GPU stack — install separately on DGX:
#   pip install cudf-cu12 cugraph-cu12 cuml-cu12 cupy-cuda12x cuopt-cu12
#   pip install torch --pre --index-url https://download.pytorch.org/whl/nightly/cu128 --no-deps
#   pip install pykeen tensorrt-llm
#
# For ollama + nemotron-mini on DGX:
#   sudo systemctl stop ollama
#   OLLAMA_NUM_GPU=999 ollama serve &
#   ollama pull nemotron-mini
